# demo_data

`demo/demo_encrypted.txt` and `demo/demo_decrypted.txt` — *Khoor iurp Vdosd!* and *Hello from
Salpa!*, written by the `hello_encrypt` and `hello_decrypt` nodes (shift 3, case name `demo`),
so they are exactly the pair this node reads to build its report. `DEMO_CONFIG` in `node.py`
points the node at `demo_data/demo` with the same case name.
