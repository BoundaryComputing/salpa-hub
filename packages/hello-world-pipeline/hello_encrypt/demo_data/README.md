# demo_data

This node takes text and a number, nothing a file could carry, so its sample is the
`DEMO_CONFIG` in `node.py`: the message *Hello from Salpa!* with a Caesar shift of 3, which
encrypts to *Khoor iurp Vdosd!* — check it by shifting each letter three places. `salpa smoke`
runs the node from that declaration.
